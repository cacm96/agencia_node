.row.proximos-viajes
            each viaje in viajes
                .col-md-6.col-lg-4.mb-4
                    .card
                        img(src=`img/destinos_${viaje.image}.jpg`, class="img-fluid")
                        .card-body
                            h2= viaje.name
                            p
                                i.fas.fa-calendar-alt
                                |  #{viaje.date_departure} - #{viaje.date_arrival}
                            p
                                i.fas.fa-dollar-sign
                                |  #{viaje.price}
                            p= viaje.description.substr(0,100)
                            a(href=`viajes/${viaje.id}`, class="btn btn-success btn-block") Más información